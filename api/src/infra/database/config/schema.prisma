generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Movie {
  id          String @id @default(uuid())
  title       String
  banner      String
  description String
  productor   String
  director    String

  @@map("movie")
}
